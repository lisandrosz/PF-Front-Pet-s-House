// import { useCustomDispatch, useCustomSelector } from 'hooks/redux';
// import { setFiltros } from 'redux/slices/mascotas';

export const useFilter = (tipoFiltro: string, valor: string): any => {
  // const dispatch = useCustomDispatch();
  // //   const estado = useCustomSelector((state) => state.pets.allPets);
  // dispatch(setFiltros( {nombre: tipoFiltro, valor: valor } )
};

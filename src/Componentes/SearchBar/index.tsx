import React from 'react';

const SearchBar: React.FC = () => {
  return (
    <div>
      SearchBar
      <input type="text" />
      <button>Buscar</button>
    </div>
  );
};

export default SearchBar;
